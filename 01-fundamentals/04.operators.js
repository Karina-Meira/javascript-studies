const currentAge = 32
const nextAge = currentAge + 1
const previousAge = currentAge - 1
const doubleAge = currentAge * 2
const halfAge = currentAge / 2
const inFiveYears = currentAge + 5
const inTenYears = currentAge + 10

console.log(`My current age is ${currentAge}`)
console.log(`Next year I will be ${nextAge}`)
console.log(`Last year I was ${previousAge}`)
console.log(`My double age is ${doubleAge}`)
console.log(`My age divided by 2 is ${halfAge}`)
console.log(`In five years I will be ${inFiveYears}`)
console.log(`In ten years I will be ${inTenYears}`)

